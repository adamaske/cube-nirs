

This is a Rubiks Cube neuroimaging project.

I was quite good at solving 3x3 when I was young, but now 15 years later I am
picking it back up. 

I want to track my progress accompanied by fNIRS data from the Prefrontal and
Occipital cortex.

I imagine a protocol like this: Rest, Plan, Solve, Rest (repeat (with 2 minutes
break and having to scramble it again myself)).


Start rest can be 20 seconds and end rest 45 seconds.

Triggers are sent to the data acquistion software via a LSL stream called "Triggers"
Triggers are sent via a custom python script, I have provided an example script,
we build out a new one. 
We need to define a table of an integer (which is sent) tied to a event/meaning
(which we use for analysis.)

I also need some sort of table to acutally fill out the protocols i actually complete.
Now I will start today with a Pilot, then talk to some people later about
improving the experimental paradigm and such. 

I want to record atleast 5 solves with the blockd esigned i descrbed above. Lets
make the python script, create a protocol.tex and report.tex, keep the folder
structure tidy.

